# Source

This fixture preserves the `externalUrl` and `retentionSize` template pattern from kube-prometheus-stack 88.2.0. The separate `templatedUrl` and `unusedTemplate` values test that only default text passed to `tpl` provides usage evidence.

Upstream project: <https://github.com/prometheus-community/helm-charts/tree/main/charts/kube-prometheus-stack>

Upstream license: Apache-2.0.
