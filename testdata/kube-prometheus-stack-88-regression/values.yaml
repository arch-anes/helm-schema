prometheus:
  prometheusSpec:
    externalUrl: ""
    retentionSize: ""
    templatedUrl: '{{ .Values.prometheus.prometheusSpec.embedded }}'
    unusedTemplate: '{{ .Values.prometheus.prometheusSpec.unusedEmbedded }}'
